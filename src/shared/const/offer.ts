export const IMAGES_QUANTITY = 6;

export enum OfferTitleLength {
  Min = 10,
  Max = 100,
}

export enum OfferDescriptionLength {
  Min = 20,
  Max = 1024,
}

export enum OfferRatingRange {
  Min = 1,
  Max = 5,
}

export enum OfferRoomsNumber {
  Min = 1,
  Max = 8,
}

export enum OfferGuestsNumber {
  Min = 1,
  Max = 10,
}

export enum OfferPriceRange {
  Min = 100,
  Max = 100000,
}
